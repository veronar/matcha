<div class="row">
	{{#each singles}}

	{{#if online}}

	<div class="col-sm-4" style="margin-bottom: 35px;">
		<div class="card">

			<img src="{{image}}" class="card-img-top">
			<div class="card-body">

				<h5>{{fullname}}</h5>

				<a href="/userProfile/{{_id}}">
					<button class="btn btn-outline-primary"><i class="fa fa-address-book"></i></button>
				</a>

				<a href="/startChat/{{_id}}">
					<button class="btn btn-outline-info"><i class="fa fa-commenting"></i></button>
				</a>

				<p class="green">Online</p>

			</div>

		</div>
	</div>

	{{else}}
	<div class="col-sm-4" style="margin-bottom: 35px;">
		<div class="card">

			<img src="{{image}}" class="card-img-top">
			<div class="card-body">

				<h5>{{fullname}}</h5>

				<a href="/userProfile/{{_id}}">
					<button class="btn btn-outline-primary"><i class="fa fa-address-book"></i></button>
				</a>

				<a href="/startChat/{{_id}}">
					<button class="btn btn-outline-info"><i class="fa fa-commenting"></i></button>
				</a>

				<p class="red">Offline</p>

			</div>

		</div>
	</div>

	{{/if}}

	{{else}}
	<p>No users found</p>
	<h1>hahahahah</h1>
	{{/each}}
</div>
